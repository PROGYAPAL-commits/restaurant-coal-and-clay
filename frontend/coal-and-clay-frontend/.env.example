# Copy to .env (or .env.local) for each dashboard if you need to override defaults.

# Backend API + Socket.IO base URL
VITE_API_URL=http://localhost:4000

# Optional: force which dashboard renders regardless of port.
# One of: customer | kitchen | owner
# VITE_APP_ROLE=customer
